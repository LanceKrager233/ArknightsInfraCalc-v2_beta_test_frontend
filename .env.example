# Local development already uses .local/auth.sqlite and http://localhost:5174.
# Production must provide both values below.
BETTER_AUTH_SECRET=replace-with-at-least-32-random-characters
BETTER_AUTH_URL=https://your-domain.example

# Optional: override the local SQLite location.
# BETTER_AUTH_DB_PATH=C:/absolute/path/to/auth.sqlite
